// import type { Car } from '../utils/types';
// import { BASE_URL } from './apiConfig';

// const WINNERS_URL = `${BASE_URL}/winners`;

// export const getAllWinners = async (): Promise<Car[]> => {
//   const response = await fetch(`${WINNERS_URL}`);

//   if (!response.ok) {
//     throw new Error(`Failed to fetch winners. Status ${response.status}`);
//   }

//   return response.json();
// };

// export const getWinnerById = async (id: number): Promise<> => {};

// export const createWinner = async
